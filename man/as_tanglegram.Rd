% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tanglegram.R
\name{as_tanglegram}
\alias{as_tanglegram}
\alias{as_tanglegram.tanglegram}
\alias{as_tanglegram.default}
\alias{as_tanglegram.cophylo}
\title{Coerce paired-tree inputs to a tanglegram}
\usage{
as_tanglegram(x, ...)

as_tanglegram.tanglegram(x, ...)

as_tanglegram.default(
  x,
  y = NULL,
  assoc = NULL,
  layout = "rectangular",
  ladderize = TRUE,
  preserve_layers = TRUE,
  ...
)

as_tanglegram.cophylo(
  x,
  ...,
  layout = "rectangular",
  ladderize = TRUE,
  preserve_layers = TRUE
)
}
\arguments{
\item{x}{A tree-like object, a \code{ggtree} plot, a \code{tanglegram}, or a \code{cophylo} object.}

\item{...}{Additional arguments passed to \code{fortify()} for raw-tree inputs.}

\item{y}{Optional second tree-like object or \code{ggtree} plot.}

\item{assoc}{Association table. It should contain \code{left} and \code{right} columns,
or two unnamed columns that will be treated as \code{left} and \code{right}.}

\item{layout}{Tree layout for raw-tree inputs. Only \code{"rectangular"} is supported in v1.}

\item{ladderize}{logical flag passed to \code{fortify()} for raw-tree inputs.}

\item{preserve_layers}{logical flag indicating whether replayable layers from
\code{ggtree} inputs should be carried over.}
}
\value{
A \code{tanglegram} object.
}
\description{
Normalize supported paired-tree inputs into a \code{tanglegram} object.
}
\examples{
tr1 <- ape::rtree(5)
tr2 <- ape::rtree(5)
tr2$tip.label <- tr1$tip.label[c(1, 3, 5, 2, 4)]
assoc <- data.frame(left = tr1$tip.label, right = tr2$tip.label)

tg <- as_tanglegram(tr1, y = tr2, assoc = assoc)
df <- fortify(tg)
}
